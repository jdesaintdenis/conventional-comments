{
  "name": "conventional-comments-helper-driveco",
  "version": "0.2.6",
  "description": "Helps format code review comments on GitHub using the Conventional Comments standard.",
  "scripts": {
    "build:chrome": "node build-manifests.js chrome",
    "build:firefox": "node build-manifests.js firefox",
    "build": "node build-manifests.js"
  },
  "dependencies": {
    "esbuild": "^0.25.9"
  }
}